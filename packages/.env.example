# Ollama
OLLAMA_HOST=http://localhost:11434
OLLAMA_MODEL=qwen3:8b

# External APIs (optional — only needed for rate_agent / cron jobs)
TAVILY_API_KEY=tvly-...

# Admin protection (set a strong password before exposing /api/admin/*)
ADMIN_PASSWORD=changeme
